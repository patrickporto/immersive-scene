# Changesets

Use Changesets to declare semantic version bumps for releasable changes.

- Create a changeset: `npm run changeset`
- Update versions locally: `npm run version-packages`
- Release publish script (CI): `npm run release` (Git tags + GitHub Releases)
